<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1a1a2e">

    <TextView
        android:id="@+id/tvDebugTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="HTTP-журнал (последние 100 запросов)"
        android:textColor="#aaaaaa"
        android:textSize="12sp"
        android:padding="8dp"
        android:background="#0d0d1a" />

    <TextView
        android:id="@+id/tvDebugEmpty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Запросов пока нет"
        android:textColor="#aaaaaa"
        android:textSize="16sp"
        android:visibility="gone" />

    <ListView
        android:id="@+id/listDebug"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvDebugTitle"
        android:divider="#0f3460"
        android:dividerHeight="1dp"
        android:visibility="gone" />

</RelativeLayout>
